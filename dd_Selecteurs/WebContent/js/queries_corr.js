function byId() {	// Select the element and store it in a variable.	var el = document.getElementById('one');	// Change the value of the class attribute.	el.className = 'cool';	console.log(el);	// var elements = document.getElementsByTagName('li'); // Find <li> elements	// var el2 = document.getElementById('four');	// console.log("value !of class "+(el2.className== ""));}function byClass() {	var elements = document.getElementsByClassName('hot'); // Find hot items	// var elements = document.querySelectorAll('.hot');	console.log(elements);	for (var i = 0; i < elements.length; i++) {		console.log(i + "  before " + elements);		var el = elements[i].className = 'cool'; // Select the third one from													// the NodeList		console.log(i + "  after " + elements.length);		// el.className = 'cool';		// el.setAttribute('class','cool');		// Change the value of its class attribute	}	console.log(i + "  after2 " + elements.length);	// if (elements.length > 1) { // If 1 or more are found	//	// var el = elements[1]; // Select the third one from the NodeList	// el.className = 'cool'; // Change the value of its class attribute	//	// }}function byTag() {	var elements = document.getElementsByTagName('li'); // Find <li> elements	console.log(elements);	if (elements.length > 2) { // If 2 or more are found		var el = elements[2]; // Select the first one using array syntax		el.className = 'cool'; // Change the value of the class attribute	}}function byQuery() {	// querySelector only returns the first match.	// var el = document.querySelector('li.hot');	// el.className = 'cool';	// querySelectorAll returns a NodeList.	// The second li element is then selected and changed.	var els = document.querySelectorAll('li.hot');	if (els.length > 1)		els[1].className = 'cool';	console.log(els);}function reset() {	var elements = document.getElementsByTagName('li'); // Find <li> elements	for (var i = 0; i < elements.length - 1; i++) {		var el = elements[i]; // Select the first one using array syntax		el.className = 'hot'; // Change the value of the class attribute	}}var nb = 0;function addElement() {	// Create a new element and store it in a variable.	var newEl = document.createElement('li');	// Create a text node and store it in a variable.	var newText = document.createTextNode('Special One');	newEl.id = 'one';	nb % 2 == 0 ? newEl.className = 'cool' : newEl.className = 'hot';	// Attach the new text node to the new element.	newEl.appendChild(newText);	// Find the position where the new element should be added.	var position = document.getElementsByTagName('ul')[0];	// Insert the new element into its position.	position.appendChild(newEl);	nb++;}function removeElement() {	// Store the element to be removed in a variable.	var removeEl = document.getElementsByTagName('li')[0];	// Find the element which contains the element to be removed.	var containerEl = document.getElementsByTagName('ul')[0];	// Remove the element.	containerEl.removeChild(removeEl);}