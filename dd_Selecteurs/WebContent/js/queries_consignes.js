function byId() {	// Select the element and store it in a variable.	// Change the value of the class attribute.}function byClass() {	// Find hot items	// If 1 or more are found	// Select the third one from the NodeList	// Change the value of its class attribute}function byTag() {	// Find <li> elements	// If 2 or more are found	// Select the first one using array syntax	// Change the value of the class attribute}function byQuery() {	// select li with .hot	// replace first el class by 'cool';}function reset() {	// Find <li> elements	// Select the first one using array syntax	// Change the value of the class attribute}var nb = 0;function addElement() {	// Create a new element and store it in a variable.	// Create a text node and store it in a variable .	var newText = document.createTextNode('Special One');	newEl.id = 'one';	nb % 2 == 0 ? newEl.className = 'cool' : newEl.className = 'hot';	// Attach the new text node to the new element.	// Find the position where the new element should be added.	// Insert the new element into its position.	nb++;}function removeElement() {	// Store the first li element to be removed in a variable.	// Find the first lu element which contains the element to be removed.	// Remove the element.}